
import { TYPES } from '../types/typesSearch'

export const searchReducer = ( state, action ) => {

    switch (action.type) {
        case TYPES.ADD_TO_CART: {



        }
    
        default:
            return state
    }

    
}
